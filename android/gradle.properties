org.gradle.jvmargs=-Xmx4096m
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Temporary AGP 9 escape hatch for plugins still using proguard-android.txt
# (e.g. flutter_inappwebview_android). Remove once plugins are updated.
android.r8.proguardAndroidTxt.disallowed=false
# Soften JVM target mismatches if a plugin still races compile options.
kotlin.jvm.target.validation.mode=warning
